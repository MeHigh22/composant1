import React from "react"
import Navigation from "../Navigation/Navigation"

const header = () => {
    return(
        <header>
            <Navigation/>
        </header>
    )
}

export default header